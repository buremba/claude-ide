# Browser Demo Configuration
# Tests browser integration with MCP Sidecar

reuse: true

browser:
  enabled: true
  headless: false
  autoOpen: true
  # storageStateDir: ./.auth  # Uncomment to persist auth state

processes:
  demo-server:
    command: node server.cjs
    port: 3000
